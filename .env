# Environment variables declared in this file are NOT automatically loaded by Prisma.
# Please add `import "dotenv/config";` to your `prisma.config.ts` file, or use the Prisma CLI with Bun
# to load environment variables from .env files: https://pris.ly/prisma-config-env-vars.

# Prisma supports the native connection string format for PostgreSQL, MySQL, SQLite, SQL Server, MongoDB and CockroachDB.
# See the documentation for all the connection string options: https://pris.ly/d/connection-strings

# The following `prisma+postgres` URL is similar to the URL produced by running a local Prisma Postgres
# server with the `prisma dev` CLI command, when not choosing any non-default ports or settings. The API key, unlike the
# one found in a remote Prisma Postgres URL, does not contain any sensitive information.

# Server
PORT=3000
NODE_ENV=development

# Database
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/learninghub?schema=public"

# JWT
JWT_SECRET="seu-secret-super-seguro-aqui"
JWT_ACCESS_TOKEN_EXPIRES_IN="15m"
JWT_REFRESH_TOKEN_SECRET="seu-refresh-token-secret-aqui"
JWT_REFRESH_TOKEN_EXPIRES_IN="7d"
JWT_VERIFICATION_TOKEN_SECRET="seu-verification-token-secret-aqui"
JWT_VERIFICATION_TOKEN_EXPIRES_IN="24h"
JWT_PASSWORD_RESET_TOKEN_SECRET="seu-password-reset-token-secret-aqui"
JWT_PASSWORD_RESET_TOKEN_EXPIRES_IN="1h"

# Redis
REDIS_URL=redis://localhost:6379

# IA (Ollama)
OLLAMA_HOST=http://172.30.208.1:11434
LLM_MODEL=gemma2:2b
EMBED_MODEL=nomic-embed-text

# Azure Blob Storage (certificados)
AZURE_STORAGE_CONNECTION_STRING=
AZURE_CONTAINER_NAME=certificates